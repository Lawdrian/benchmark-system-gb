/**
 * #############################################################################
 * LayoutConfig.ts: Configures the application layout
 * #############################################################################
 */
import {LayoutConfig} from "../types/SharedLayoutTypes";

const layoutConfig: LayoutConfig = {
    drawerWidth: 240
}

export default layoutConfig;